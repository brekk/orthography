import { report } from "Study"

import {
  camel,
  constant,
  dot,
  kebab,
  pascal,
  pascalSnake,
  path,
  sentence,
  snake,
  train,
  validate,
} from "@/Pairs"



report(
  validate(camel),
  "validate - camel",
  [
    #["skibble", true],
    #["zipZapZop", true],
    #["", true],
    // camel doesn't have spaces, so this doesn't behave transparently
    #["hey There", false],
  ],
)
